paths:
 /api/users/:
  get:
   summary: get all users
   tags:
    - Users
  responses:
   "200":
     description: User successfully obtained
   "500":
     description: Internal error of server, usually for a bad query or bad connexion